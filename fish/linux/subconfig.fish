# brew
set PATH $PATH ~/.linuxbrew/bin
set PATH $PATH /home/linuxbrew/.linuxbrew/bin

# rust/cargo
set PATH $PATH ~/.cargo/bin
set CARGO_TARGET_DIR ~/.cargo/global_target

# pnpm
set PNPM_HOME ~/.local/share/pnpm/store/v3/
set PATH $PATH $PNPM_HOME
